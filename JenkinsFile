pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker build -t neneobianodo/pythonimage .'
            }
        }
        
        stage('Test'){
            steps{
                // Add your test steps here. For example:
                sh 'docker run --name my-marianene-app-container -d -p 8001:8000 neneobianodo/pythonimage'
                sh 'curl localhost:8001'
                sh 'docker stop my-marianene-app-container && docker rm my-marianene-app-container'
            }
        }

        stage('Deploy') {
            steps {
                sh 'docker run -d -p 8000:8000 --name=my-marian1-app-container neneobianodo/pythonimage'
            }
        }
    }
}


