pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker build -t neneobianodo/pythonimage .'
            }
        }
        
        stage('Test'){
            steps{
                // Add your test steps here. For example:
                sh 'docker run --name my-nenetb-app-container -d -p 8007:8000 neneobianodo/pythonimage'
                sh 'curl localhost:8007'
                sh 'docker stop my-nenetb-app-container && docker rm my-nenetb-app-container'
            }
        }

        stage('Deploy') {
            steps {
                sh 'docker run -d -p 8000:8000 --name=my-nenetb-app-container neneobianodo/pythonimage'
            }
        }
    }
}


