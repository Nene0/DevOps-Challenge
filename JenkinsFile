pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker build -t neneobianodo/pythonimage .'
            }
        }
        
        stage('Test'){
            steps{
                // Add your test steps here. For example:
                sh 'docker run --name my-nenetb2-app-container -d -p 8008:8000 neneobianodo/pythonimage'
                sh 'curl localhost:8008'
                sh 'docker stop my-nenetb2-app-container && docker rm my-nenetb2-app-container'
            }
        }

        stage('Deploy') {
            steps {
                sh 'docker run -d -p 8000:8000 --name=my-nenetb2-app-container neneobianodo/pythonimage'
            }
        }
    }
}


